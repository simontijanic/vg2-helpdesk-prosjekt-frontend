<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="fw-bold navbar-brand" href="/">Helpdesk System</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="/guide">Brukermanual</a>
        </li>
        <% if (locals.user) { %>
          <% if (locals.user.role === "admin") { %>
            <li class="nav-item">
              <a class="nav-link" href="/dashboard">Admin Dashboard</a>
            </li>
          <% } else if (locals.user.role === "first-line" || locals.user.role === "second-line") { %>
            <li class="nav-item">
              <a class="nav-link" href="/tickets">Support Dashboard</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/tickets/create">Create Ticket</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/tickets">My Tickets</a>
            </li>
          <% } %>
          <li class="nav-item">
            <span class="fw-bold nav-link text-light"><%= locals.user.email %></span>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logg Ut</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/login">Logg Inn</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/register">Registrer Deg</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>